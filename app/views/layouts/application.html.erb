<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Empower Monitor" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag 'favicon.png' %>

    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    
    <style>
      :root {
        --color-empower-pink: #E91E63;
        --color-vibrant-magenta: #C2185B;
        --color-deep-purple: #8E24AA;
        --color-dark-violet: #6A1B9A;
        --glass-bg: rgba(255, 255, 255, 0.08);
        --glass-border: rgba(255, 255, 255, 0.15);
        --glass-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.4);
      }

      body {
        background: linear-gradient(135deg, #120524 0%, var(--color-dark-violet) 50%, var(--color-deep-purple) 100%);
        background-attachment: fixed;
        min-height: 100vh;
        font-family: 'Open Sans', sans-serif;
        color: white;
      }

      h1, h2, h3, h4, h5, h6, .navbar-brand {
        font-family: 'Montserrat', sans-serif;
      }

      /* Glassmorphism Utilities */
      .glass-panel {
        background: var(--glass-bg);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border: 1px solid var(--glass-border);
        box-shadow: var(--glass-shadow);
        border-radius: 20px;
      }

      .navbar {
        background: rgba(18, 5, 36, 0.7) !important;
        backdrop-filter: blur(12px);
        border-bottom: 1px solid var(--glass-border);
      }
      
      .navbar-brand {
        font-weight: 700; 
        letter-spacing: 1px;
        color: white !important;
        text-shadow: 0 0 15px var(--color-empower-pink);
        font-size: 1.5rem;
      }
      
      .btn-primary {
        background: linear-gradient(45deg, var(--color-empower-pink), var(--color-vibrant-magenta));
        border: none;
        box-shadow: 0 4px 15px rgba(233, 30, 99, 0.4);
        transition: all 0.3s ease;
      }
      
      .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(233, 30, 99, 0.5);
        background: linear-gradient(45deg, var(--color-vibrant-magenta), var(--color-empower-pink));
      }
    </style>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-dark mb-4">
      <div class="container">
        <a class="navbar-brand" href="/">
          <i class="bi bi-activity"></i> Empower Monitor
        </a>
      </div>
    </nav>

    <div class="container">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <%= yield %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>